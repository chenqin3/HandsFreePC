"""Task-oriented desktop control for the opt-in assistive engine.

The controller is deliberately not imported here: configuration keeps the
proof engine import graph unchanged and loads assistive_v1 only when selected.
"""
